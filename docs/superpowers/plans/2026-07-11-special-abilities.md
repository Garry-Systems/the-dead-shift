# Special Abilities "Company Equipment" (v0.1.70) — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Every character gets one signature active ability on a flat-cooldown HUD button (7 abilities), plus a NEW 7th character — Jackson Killa, heavy-weapons specialist with the SENTRY TURRET — and the character-select scroll fix his row requires.

**Architecture:** A pure `Abilities.gd` registry (character id → ability row) + one `AbilityController.gd` run-scoped node (cooldown machine + per-ability cast functions that call existing seams) + an `AbilityButton` Control in the code-built Hud. Effects reuse: projectile-purge loop, `apply_knockback` channel, `Shockwave.flash/blast`, `Engine.time_scale` (via a new `Juice.base_scale` owner), `apply_freeze`, `add_frenzy`/`instant_reload`, the Janitor slick HazardZone cfg, `CompanionBullet`, the XpGem pickup pattern (→ new HealthPack), and the Enemy.gd kill-coin site (Blood-Moon per-kill idiom).

**Tech Stack:** Godot 4.6 GDScript; home generators for 7 icons + 1 turret sprite + 6 WAVs.

**Spec:** `docs/superpowers/specs/2026-07-10-special-abilities-design.md` (approved 2026-07-11).

## Global Constraints

- Runner env / boot-scene probes (never `--script`; new class_name → editor cache pass first) / MANDATORY DUAL GATE per task with the **case-insensitive parent-parity convention** (baselines 19 editor / 7 boot known XpGem noise — state both counts, yours must be ≤ parent) / **LITERAL RED+GREEN probe output + gate numbers pasted in the report** / probe files deleted, `.uid` tracked / master, NO push before ship. `GODOT="$(find /tmp/godot46 -name '*console.exe' | head -1)"; PROJ='C:\Users\thela\Documents\mobile-game'`. ⚠️ The WSL→Windows exe LOSES piped stdout — ALWAYS `> file 2>&1` then grep the file (v0.1.69 lesson).
- **Verified seams (re-verify locally as touched):** Player dash double-tap = `_unhandled_input` (Player.gd:176) → a GUI Button that receives the tap fully blocks accidental dashes, NO extra swallow logic needed; VirtualJoystick reads raw `_input` but a stationary tap never sets a direction (drag-only) — a drag STARTING on the button moves the player exactly like today's pause button, accepted precedent. `Shockwave.blast(radius, damage, force, gun, player, hit_destructibles:=false)` (Shockwave.gd:25 — gun/player may be null, but it ALWAYS damages: CLEAR OUT must NOT use blast; drive `apply_knockback` directly + `Shockwave.flash(radius)` for the visual, the purge-FX precedent at Player.gd:~238). Ryan's current dash purge: `Player._on_dash_started` match arm `"purge"` → `_purge_projectiles()` (Player.gd:~210/227, internal `_ability_cd` + `CHAR_RYAN_ABILITY_COOLDOWN 15`); `Characters.dash_ability("ryan")` returns `"purge"` (Characters.gd:126-137); Hud's legacy `_ability_label` (Hud.gd:20,136-143,159,280-283). Kill-coin site: Enemy.gd:584-585 (`RunStats.coins_per_kill` + the `NightEvents.blood_moon_coins(tree)` group-lookup idiom at NightEvents.gd:108-116). Janitor slick cfg dict: Player.`_spawn_slick` (dps 0, hurts_player false, slow/slow_dur — HazardZone.configure_hazard). Drone shot: `Companion._fire_drone_shot` (CompanionBullet fields: `direction`, `damage`, `max_travel`, `trait_id`; raw take_damage, no talents). `Juice._on_timer_done`/`_exit_tree` hardcode `Engine.time_scale = 1.0` (Juice.gd:52-56,29-31). `Gun.add_frenzy(pct, duration)` maxf-merge (Gun.gd:180), `Gun.instant_reload()` (:325). `Enemy.apply_freeze(duration)` (:286), `apply_knockback` channel. XpGem pattern (XpGem.gd, 28 lines; enemies spawn it via `@export xp_gem_scene` — AIR DROP preloads the same scene path, verify in Enemy.tscn). `CombatText.callout(world_pos, word, color)` (:63). Char select: `MainMenu._build_char_panel` (:456, `_char_vbox = _card_vbox(_char_panel, 14)`), drag-scroll resolver chain in `MainMenu._input` (:692-698, `_inv_scroll`/`_store_scroll` pattern + `_guarded` tap-suppress helper). `RunConfig.character_id`; Main.gd:66-68 run-start character wiring. Weapons.gd has `name_for` but NO category accessor — add `category_for` (T3). Heavy ids: `minigun`, `grenade_launcher`, `lmg`.
- **Spec constants (verbatim starter values, ALL land in GameConfig in T1):** cooldowns CLEAR OUT 40 / TURRET 45 / DEAD EYE 50 / ONE OF THEM 45 / JACKPOT 60 / CLOSING TIME 45 / AIR DROP 40; CLEAR OUT push radius 520 force 1400 (zero damage); TURRET lifetime 12 interval 0.5 damage 26 range 620 cap 1 replace; DEAD EYE duration 3 scale 0.3 move-comp 2.5 frenzy 0.6; GHOST duration 4 (bosses unaffected); JACKPOT 4 equal rolls — NUKE radius 480 dmg 220 force 1400 (WITH gun talents — the Alstar crit-blast precedent Larry kept), DEEP FREEZE 3s, PAYDAY +2/kill 10s, TRIGGER HAPPY instant reload + frenzy 0.4/6s; CLOSING TIME radius = slick×3, 8s, +2/kill inside; AIR DROP delay 1.5 radius 300 dmg 160 force 1200, 2 HealthPacks (heal 25) + 3 gems (value 3); Jackson price 3600, heavy +25% dmg +15% fire rate.
- All numbers = GameConfig consts with `##` comments; copy ≤ 70 chars deadpan; palette strict (button/FX in C4 lavender family, PixelTheme); every static gameplay flag resets in `_ready` (the RelicEffects lesson); pause-contract on any SceneTreeTimer (`process_always` chosen deliberately + commented); abilities are RUN-SCOPED — nothing persists to SaveManager except Jackson's unlock via the existing character-purchase path.

---

### Task 1: Ability core — registry, controller, HUD button

**Files:** Create `scripts/logic/Abilities.gd` (pure, autoload-free — the Characters.gd header discipline), `scripts/AbilityController.gd`, `scripts/ui/AbilityButton.gd`; Modify `scripts/logic/GameConfig.gd` (the FULL `ABILITY_*` + `CHAR_JACKSON_*` block from Global Constraints — later tasks only READ it), `scripts/Hud.gd` (build + poll the button), `scenes/Main.tscn` (+AbilityController node, Juice/Visitors sibling idiom).

**Interfaces:** GameConfig const names (exact — every later task reads these): `ABILITY_CLEAROUT_CD/RADIUS/FORCE`, `ABILITY_TURRET_CD/LIFETIME/INTERVAL/DAMAGE/RANGE`, `ABILITY_DEADEYE_CD/DURATION/SCALE/MOVE_COMP/FRENZY`, `ABILITY_GHOST_CD/DURATION`, `ABILITY_JACKPOT_CD/NUKE_RADIUS/NUKE_DAMAGE/NUKE_FORCE/FREEZE_DUR/PAYDAY_COINS/PAYDAY_DURATION/FRENZY/FRENZY_DUR`, `ABILITY_CLOSING_CD/RADIUS_MULT/DURATION/COINS`, `ABILITY_AIRDROP_CD/DELAY/RADIUS/DAMAGE/FORCE/HEAL/PACKS/GEMS/GEM_VALUE`, `ABILITY_BUTTON_SIZE`, `CHAR_JACKSON_HEAVY_DMG_PCT/FIRE_PCT` (values in Global Constraints). `Abilities.for_character(id: String) -> Dictionary` — `{ "id": "clear_out", "name": "CLEAR OUT", "cd": GameConfig.ABILITY_CLEAROUT_CD }`, `{}` for unknown ids; rows: ryan→clear_out, jackson→turret SENTRY TURRET, jimbo→dead_eye DEAD EYE, bob→ghost ONE OF THEM, alstar→jackpot JACKPOT, janitor→closing_time CLOSING TIME, delivery_girl→air_drop AIR DROP. `AbilityController` (**`class_name AbilityController`** — T7's static query needs it; new class_name → editor cache pass before the boot gate): group `"ability_controller"`, reads `RunConfig.character_id` in `_ready`; API `ability_row() -> Dictionary`, `is_ready() -> bool`, `cooldown_fraction() -> float` (0 ready … 1 just cast), `try_cast() -> bool` (false while cooling; on success: cooldown restart + `CombatText.callout(player pos, name + "!", PixelTheme.ACCENT)` + `SoundManager.play("ability_ready")`-family SFX per T9 ids [play "ui_tap" until T9 lands — staged, comment it] + `match` dispatch to `_cast_<id>()`); THIS task ships all 7 `_cast_*` as callout-only bodies with `## effect lands in Task N` comments — the button/cooldown loop is fully playable. `AbilityButton extends Button`: 148px square, anchors bottom-right (offsets ≈ left/right −196/−48, top/bottom −480/−332 — starter, F5 placement pass), `_draw()` = bottom-up cooldown fill (C4 low-alpha) + ready flash; icon texture `res://art/abilities/<id>.png` via `ResourceLoader.exists` staged fallback = ability initial letter label (T9 lands art). Hud builds it only when `Abilities.for_character(RunConfig.character_id)` is non-empty, polls `cooldown_fraction()` in the existing `_process`, `pressed` → `try_cast()`; a denied press (still cooling) = a small 0.1s button nudge/offset tween, NO sound (spec: no spam).

- [ ] Probe (RED first): registry — all 7 character ids of `Characters.all()` return non-empty rows, names non-empty, every `cd > 0`, unknown id → `{}`; controller cooldown machine on a bare instance (`try_cast` true→false→tick past cd→true; `cooldown_fraction` monotonic 1→0); dispatch reaches each `_cast_*` (callout counting via a probe flag is fine). Gates parent parity. Commit `feat(abilities): registry + controller + HUD cooldown button (all casts staged)`.

---

### Task 2: CLEAR OUT + Ryan dash rework

**Files:** Modify `scripts/AbilityController.gd` (`_cast_clear_out`), `scripts/Player.gd` (delete `_purge_projectiles` + `_ability_cd` + its tick; dash match arm `"purge"` → `"ak_reload"`: `if gun != null and gun.weapon_id == "ak47": gun.instant_reload()`), `scripts/logic/Characters.gd` (`dash_ability("ryan")` → `"ak_reload"`; ryan desc → dash reloads the AK, CLEAR OUT wipes the map), `scripts/Hud.gd` (DELETE the legacy `_ability_label` block — :20,136-143,159,280-283 — superseded by the button), `scripts/logic/GameConfig.gd` (delete `CHAR_RYAN_ABILITY_COOLDOWN`; KEEP `CHAR_RYAN_PURGE_FX_RADIUS`, the FX still uses it).

**Interfaces:** `_cast_clear_out()`: free every `"enemy_projectiles"` node (is_instance_valid-guarded — the old loop verbatim), then for `"enemies"` within `ABILITY_CLEAROUT_RADIUS` of the player: `apply_knockback(dir * ABILITY_CLEAROUT_FORCE)` (has_method-guarded — bosses without the channel are skipped free) — **ZERO damage, do NOT use Shockwave.blast** (it always damages; see Global Constraints); visuals = `ScreenFlash.new()` + `Shockwave.flash(CHAR_RYAN_PURGE_FX_RADIUS)` + SFX `"purge"` (exact purge-FX recipe from the deleted function). Ryan's dash keeps DASH_COOLDOWN gating only.

- [ ] Probe: stub enemies in/out of 520px → knockback applied only inside, zero take_damage calls (stub records both); projectiles group emptied; `Characters.dash_ability("ryan") == "ak_reload"`; grep-assert `_purge_projectiles` and `CHAR_RYAN_ABILITY_COOLDOWN` have zero references left. Gates parent parity. Commit `feat(abilities): CLEAR OUT — map purge + radial push moves off Ryan's dash`.

---

### Task 3: Jackson Killa + character-select scroll

**Files:** Modify `scripts/logic/Characters.gd` (+row after delivery_girl: `id "jackson"`, name "Jackson Killa", price 3600, desc — turret + heavy bonus, ≤70-char lines; `apply_weapon` arm: `if Weapons.category_for(weapon_id) == "Heavy": upgrade_damage(CHAR_JACKSON_HEAVY_DMG_PCT); upgrade_fire_rate(CHAR_JACKSON_HEAVY_FIRE_PCT)`; `dash_ability` → `""` — plain dash), `scripts/logic/Weapons.gd` (`static func category_for(id: String) -> String` — mirror `name_for`), `scripts/MainMenu.gd` (ScrollContainer: in `_build_char_panel` wrap `_char_vbox` following the store list's construction — new `_char_scroll: ScrollContainer` member; register in the `_input` drag resolver chain at :692-698 as `elif _char_panel.visible and _char_scroll != null: sc = _char_scroll`; wrap char row button callbacks in `_guarded` — the store precedent — so a drag-release never selects a character).

**Interfaces:** `Weapons.category_for(id) -> String` (empty for unknown). The store character-purchase path lists `Characters.all()` — Jackson appears + is buyable with NO store edits (verify while in there; if the store hardcodes a character list anywhere, that's a plan bug — STOP and report). No player art: character rows are text; the in-run player sprite is shared.

- [ ] Probe: `Characters.get_character("jackson")` complete + price 3600; `category_for` on all 21 weapon ids (exactly minigun/grenade_launcher/lmg → "Heavy"); `apply_weapon` on a stub player+gun with an lmg applies both bumps, with an ak47 applies none; roster count 7. Select-panel scroll + buy flow = review/F5. Gates parent parity. Commit `feat(character): Jackson Killa — heavy specialist + char-select scroll`.

---

### Task 4: SENTRY TURRET

**Files:** Create `scripts/Turret.gd`; Modify `scripts/AbilityController.gd` (`_cast_turret`), `scripts/Basement.gd` (descend/ascend teleport site: free the `"player_turret"` group — the COWORKER_LEASH_SNAP class of fix, stationary hardware can't ride a teleport).

**Interfaces:** `Turret extends Node2D`, group `"player_turret"`; `static func spawn(pos: Vector2, tree: SceneTree) -> void` frees any existing group member first (cap 1 REPLACE — the MannequinDecoy.spawn idiom), adds to `tree.current_scene`; `_process`: lifetime `ABILITY_TURRET_LIFETIME` countdown (expire = queue_free + small flash), every `ABILITY_TURRET_INTERVAL` retarget nearest `"enemies"` node within `ABILITY_TURRET_RANGE` (Companion.`_nearest_enemy` logic reimplemented locally — no Companion dependency) and fire a `CompanionBullet` exactly like `_fire_drone_shot` (`direction`/`damage = ABILITY_TURRET_DAMAGE`/`max_travel = ABILITY_TURRET_RANGE`/`trait_id = ""` — talent-free per spec); visual = staged sprite `res://art/abilities/turret_prop.png` + `_draw` fallback (C3 body / C4 barrel, the Companion fallback idiom). `_cast_turret()`: `Turret.spawn(player.global_position, get_tree())`.

- [ ] Probe: spawn → group size 1; second spawn → still 1 and it's the NEW instance; lifetime tick → freed; fire tick on a stub enemy constructs a CompanionBullet with the exact 4 fields (probe the bullet's fields, then free it); basement sweep — node in group + the Basement free call → gone. Gates parent parity. Commit `feat(abilities): SENTRY TURRET — Jackson's cap-1 auto-turret`.

---

### Task 5: DEAD EYE

**Files:** Modify `scripts/Juice.gd` (`static var base_scale := 1.0`, reset in `_ready` [statics survive reloads — the RelicEffects lesson]; `_on_timer_done` restores `Engine.time_scale = base_scale`; `_exit_tree` restores 1.0 AND `base_scale = 1.0` — scene teardown is a true reset), `scripts/AbilityController.gd` (`_cast_dead_eye` + `_end_dead_eye`).

**Interfaces:** `_cast_dead_eye()`: `Juice.base_scale = ABILITY_DEADEYE_SCALE; Engine.time_scale = ABILITY_DEADEYE_SCALE`; player comp: `player.move_speed *= ABILITY_DEADEYE_MOVE_COMP` (store the ratio for exact revert — multiplicative, commutes with mid-window cards) + `player.gun.add_frenzy(ABILITY_DEADEYE_FRENZY, ABILITY_DEADEYE_DURATION)` (maxf-merge — safe with Bloodrush); end via `get_tree().create_timer(ABILITY_DEADEYE_DURATION, false, false, true)` — **process_always=false (pause-safe: a paused game holds the window) + ignore_time_scale=true (3 REAL seconds), comment the pause-contract** — → `_end_dead_eye()`. `_end_dead_eye()` idempotent (guard flag): `Juice.base_scale = 1.0; Engine.time_scale = 1.0`; revert move ratio. Safety nets: connect `player.died` → `_end_dead_eye`; `AbilityController._exit_tree` → `_end_dead_eye` (mirrors Juice's net — quitting to menu mid-window can NEVER strand 0.3). Hit-stop during the window now freezes to 0.05 then restores to 0.3 — that's the point of base_scale.

- [ ] Probe: `Juice.base_scale` default + `_on_timer_done(matching token)` restores to a probe-set 0.3 (direct call, stub instance); `_end_dead_eye` idempotency (double-call reverts move_speed exactly once — stub player); died-signal path restores 1.0. Gates parent parity. Commit `feat(abilities): DEAD EYE — bullet time with a base_scale owner in Juice`.

---

### Task 6: ONE OF THEM (ghost seam)

**Files:** Modify `scripts/Player.gd` (`_ghost_time` + `func is_ghost() -> bool` + countdown in `_physics_process`; ghost tint on the player sprite while active — C4 at low alpha, restore after), `scripts/Enemy.gd` (`func _target_ghosted() -> bool` — `_target != null and is_instance_valid(_target) and _target.is_ghost()`; gate the chase-dir recompute at ~:535 on `not _target_ghosted()` [velocity persists → they walk their last heading, reads as "lost you" — do NOT zero velocity, that's frozen's look]; gate the contact-damage block at ~:522 on `not _target_ghosted()`; statuses/taunt untouched — a taunted enemy already isn't targeting the player), `scripts/RangedEnemy.gd` (`_act` top: `if _target_ghosted(): return` — hold fire), `scripts/AbilityController.gd` (`_cast_ghost`: `player.set_ghost(ABILITY_GHOST_DURATION)`).

**Interfaces:** `Player.set_ghost(duration: float)` / `is_ghost()`. HiveEnemy keeps spawning broods (broods themselves ignore — they're Enemies); ExploderEnemy — verify its detonation trigger path while in there: if proximity/contact drives it, the two Enemy gates already cover it; if it self-triggers elsewhere, gate that site the same way and REPORT. Bosses: BossBase has no `_target_ghosted` — unaffected by construction, per spec ("they know").

- [ ] Probe: `set_ghost`/`is_ghost` window ticks out; `_target_ghosted` truth table on an Enemy with a stub target (ghost on/off/null/freed); RangedEnemy `_act` early-return (probe flag on a stub fire method — restructure the gate so the check is a plain method if needed). Contact/movement gates = review + F5 (slide collisions aren't probeable headless). Gates parent parity. Commit `feat(abilities): ONE OF THEM — the horde forgets Bob exists`.

---

### Task 7: JACKPOT + the per-kill coin window seam

**Files:** Modify `scripts/AbilityController.gd` (`_cast_jackpot` + the kill-coin window state + static query), `scripts/Enemy.gd` (ONE line at the :584 kill-coin site: after the Janitor/blood-moon bonuses, `RunStats.add_coins(AbilityController.kill_coin_bonus(global_position, get_tree()))` — guard `> 0` like its neighbors).

**Interfaces:** `AbilityController.kill_coin_bonus(pos: Vector2, tree) -> int` — STATIC, group-lookup idiom copied from `NightEvents.blood_moon_coins` (NightEvents.gd:108-116): finds the controller via `"ability_controller"`, returns its live window bonus — `ABILITY_JACKPOT_PAYDAY_COINS` while a time window is active, `ABILITY_CLOSING_COINS` while a ZONE window is active AND `pos` is inside it (zone fields land here, CLOSING TIME arms them in T8), else 0; windows stack additively if both run. Instance fields: `_coin_window_until: float`, `_coin_zone_center: Vector2`, `_coin_zone_radius: float`, `_coin_zone_until: float` (engine-clock seconds, the `_last_shift_toast` idiom — NO static gameplay state beyond the stateless query). `_cast_jackpot()`: `randi() % 4` (player-triggered RNG is un-seeded by design — the loot-RNG rule, Daily unaffected) → 0 NUKE: `Shockwave.blast(ABILITY_JACKPOT_NUKE_RADIUS, ABILITY_JACKPOT_NUKE_DAMAGE, ABILITY_JACKPOT_NUKE_FORCE, player.gun, player, true)` (WITH talents — the kept Alstar crit-blast precedent); 1 DEEP FREEZE: every `"enemies"` node `has_method("apply_freeze")` → `apply_freeze(ABILITY_JACKPOT_FREEZE_DUR)` (bosses lack the method = immune, free); 2 PAYDAY: `_coin_window_until = now + ABILITY_JACKPOT_PAYDAY_DURATION`; 3 TRIGGER HAPPY: `gun.instant_reload()` + `gun.add_frenzy(ABILITY_JACKPOT_FRENZY, ABILITY_JACKPOT_FRENZY_DUR)`. Each roll gets its own `CombatText.callout` ("JACKPOT: DEEP FREEZE!" — the cast's generic callout is REPLACED by the roll callout for this ability).

- [ ] Probe: 4000 rolls ≈ equal quarters (probe the match through a seeded local RNG injected for the probe, or extract the roll to `_roll_jackpot(r: int)` and drive all 4 arms directly — the arms are the test target, drive them directly); each arm's seam calls on stubs (freeze called per enemy, reload+frenzy fields, window set); `kill_coin_bonus` truth table (no controller in tree → 0; time window active → 2, expired → 0; zone active + inside → 2, outside → 0; both → 4). Gates parent parity. Commit `feat(abilities): JACKPOT — four-roll slot machine + per-kill coin window seam`.

---

### Task 8: CLOSING TIME + AIR DROP (+ HealthPack)

**Files:** Create `scripts/HealthPack.gd`, `scripts/AirDropMarker.gd`; Modify `scripts/AbilityController.gd` (`_cast_closing_time`, `_cast_air_drop`).

**Interfaces:** `_cast_closing_time()`: one HazardZone at the player — the `_spawn_slick` cfg dict verbatim EXCEPT `radius = CHAR_JANITOR_SLICK_RADIUS * ABILITY_CLOSING_RADIUS_MULT`, `duration = ABILITY_CLOSING_DURATION` (dps 0 / hurts_player false / slow reuses `CHAR_JANITOR_SLICK_SLOW`); do NOT ride `cap_player_pools` (the giant zone must not evict itself or the Janitor's own dash slicks — the TrailDash own-group precedent; cooldown IS its cap); arm the T7 zone window: `_coin_zone_center = player pos; _coin_zone_radius = the zone radius; _coin_zone_until = now + ABILITY_CLOSING_DURATION`. `HealthPack extends Node2D` — XpGem.gd copied structurally (group `"health_packs"`, drift inside `pickup_radius`, collect at `GEM_COLLECT_DISTANCE` → `player.heal(ABILITY_AIRDROP_HEAL)` + SFX `"gem"` until T9; `heal()` already carries the hardcore/blood_pact gates — NO special-casing, comment it). `AirDropMarker extends Node2D`: `_draw` telegraph ring (C4, radius `ABILITY_AIRDROP_RADIUS`) for `ABILITY_AIRDROP_DELAY` (pause-safe SceneTreeTimer, process_always=false, comment), then: `Shockwave.new()` + `blast(ABILITY_AIRDROP_RADIUS, ABILITY_AIRDROP_DAMAGE, ABILITY_AIRDROP_FORCE, null, null, true)` (null gun = no talents — supported, verified; hit_destructibles true — a crate under the drop bursts), spawn `ABILITY_AIRDROP_PACKS` HealthPacks + `ABILITY_AIRDROP_GEMS` XpGems (`value = ABILITY_AIRDROP_GEM_VALUE`; preload the same XpGem scene Enemy.tscn exports — verify the path) scattered ~60px, then free. `_cast_air_drop()`: marker at player position snapshot.

- [ ] Probe: closing-time cfg dict field-exact vs `_spawn_slick`'s (radius/duration excepted) + zone window armed (T7 query returns 2 inside / 0 outside); HealthPack collect → heal called with 25 on a stub player + frees; hardcore path: `RunConfig.hardcore = true` stub → heal no-ops (assert via the stub's health unchanged — restore the flag after, probe hygiene); AirDropMarker sequencing (telegraph → blast fields → spawn counts on stubs — extract `_land()` and drive it directly). Gates parent parity. Commit `feat(abilities): CLOSING TIME + AIR DROP — zone economy and the first HealthPack`.

---

### Task 9: Art + audio + wiring polish

**Files:** Home repo — Modify `gen_palette_sprites.py` (new `abilities()`: 7 × 64px button icons `art/abilities/{clear_out,turret,dead_eye,ghost,jackpot,closing_time,air_drop}.png` — C3/C4 pictograms (ring-pulse, turret silhouette, eye, fading figure, three-reel, wet-floor sign, falling crate) + 1 × 32px `turret_prop.png`; palette strict), `gen_retro_audio.py` (6 WAVs: `ability_ready` [bright two-note], `ability_turret` [servo clunk], `ability_deadeye` [descending whoosh], `ability_ghost` [airy veil], `ability_jackpot` [slot-reel triplet], `ability_airdrop` [whistle + thud]). Game repo — copy outputs, Modify `scripts/SoundManager.gd` (+6 ids in `_IDS`), `scripts/AbilityController.gd` (per-ability SFX ids replace the staged `ui_tap`; CLEAR OUT keeps `"purge"`), `scripts/ui/AbilityButton.gd` (icons resolve now — fallback letter stays as the staged path).

**Interfaces:** Multimodal contact-sheet QA on the 8 sprites (the controller-iteration convention — icons must read at 148px AND stay distinct); WAV register + `.import` files committed (the v0.1.67 audio precedent). Commit home repo generators separately (home has no remote — local commit only).

- [ ] Probe: none (asset task) — boot gate + editor gate at parent parity prove the registrations; REPORT the contact sheet. Commit `art+audio(abilities): 7 icons, turret prop, 6 ability WAVs registered`.

---

### Task 10: Ship

- [ ] Full-run smoke via the dual gates at parent parity; `VERSION` → `0.1.70` (BUMP OR THE APK LIES — v0.1.60 lesson); CHANGELOG entry "v0.1.70 — Company Equipment" (player-facing, deadpan); push master → CI run 70 green → download `android-latest` APK → `git tag v0.1.70` at the built commit → `gh release create v0.1.70 <apk> --title "v0.1.70 — Company Equipment"`. F5 checklist for Larry: button placement vs thumbs; mash the button — must never dash; each of the 7 casts fires + reads; DEAD EYE can never leave the game slowed (pause / die / quit mid-window); turret expires in the basement; JACKPOT rolls all 4 over a session; select panel scrolls to Jackson and he's buyable at 3600; kills inside CLOSING TIME visibly pay extra.
